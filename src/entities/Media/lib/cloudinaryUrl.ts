/**
 * Cloudinary URL utilities
 *
 * URL variants (thumbnail, watermarked lightbox) are now pre-generated as Cloudinary
 * eager transforms at upload time and stored directly in the DB.
 * This module is intentionally empty — kept to avoid any stale import errors
 * during migration. Remove once all references are confirmed clean.
 */
