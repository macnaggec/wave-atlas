/**
 * HomePage - Globe-first landing experience
 *
 * The globe and spots are rendered by GlobeLayout.
 * This component handles any page-specific floating UI
 * that appears on the main route.
 *
 * Drawer is now handled by parallel routes (@drawer slot).
 *
 * Future additions:
 * - Floating search bar
 * - Date selection
 */
export default function HomePage() {
  return null;
}
