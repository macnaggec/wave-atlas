/**
 * Default for @drawer parallel route slot
 *
 * Returns null when no drawer route is active.
 * This ensures the drawer only appears when navigating to spot routes.
 */
export default function Default() {
  return null;
}
